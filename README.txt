Mithra Hotel / FOODIE POS V6
FIXED VERSION

Important:
1. Replace the old index.html in GitHub with this index.html.
2. This version keeps all POS JavaScript inside index.html.
3. Plus/minus uses event delegation, so Android touch taps work reliably.
4. No PIN is used for normal +, -, Bill, Cash or UPI actions.
5. Cart, sales and current page are saved in LocalStorage.
6. After deployment, open the GitHub Pages URL with ?v=6 once to bypass an old browser cache.
